export interface PartnerCenter {
  id: number;
  name: string;
  phoneNumber: string;
  phoneCode: string;
  contactEmail: string;
  privatized: boolean;
  code: string;
  address: string;
  detailsAddress: string;
  postalCode: string;
  city: string;
  created: Date;
  updated: Date;
}
