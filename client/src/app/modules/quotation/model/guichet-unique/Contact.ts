export interface Contact {
  roleDonneesContact: string; // TODO : pas de référentiel associé ? comment remplir ?
  mail: string;
  telephone: string; // Format ?
  telecopie: string;
  optionRgpd: string;
  dateEffet: Date;
  phoneCode: string; // TODO : pas de référentiel associé ? Format
}

